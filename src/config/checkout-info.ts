export interface CheckoutInfo {
  firstName: string;
  lastName: string;
  postalCode: string;
}

export const checkoutInfo: CheckoutInfo = {
  firstName: 'John',
  lastName: 'Doe',
  postalCode: '12345',
};

